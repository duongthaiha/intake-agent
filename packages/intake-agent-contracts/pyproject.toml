[project]
name = "intake-agent-contracts"
version = "1.0.0"
description = "Versioned agent-neutral MCP contracts for Intake Agent"
requires-python = ">=3.12,<3.15"
dependencies = ["pydantic>=2.11.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
